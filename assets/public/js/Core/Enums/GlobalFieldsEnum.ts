/**
 * Enum, that contains games Strategies
 */
export const enum GlobalFieldsEnum {
    GAME_TYPE = 'g_game_type',

    // Canvas
    HEIGHT_RATIO = 'g_height_ratio',
    WIDTH_RATIO = 'g_width_ratio',

    // Buttons
    BTNS_BG = 'g_btns_bg',
    BTNS_COLOR = 'g_btns_color',
    BTNS_RADIUS = 'g_btns_radius',

    // Start Screen
    START_SCREEN_CONTENT = 'startscreen_content'
}